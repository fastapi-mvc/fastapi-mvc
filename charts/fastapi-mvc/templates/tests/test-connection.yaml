apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fastapi-mvc.fullname" . }}-test-connection"
  labels:
    {{- include "fastapi-mvc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fastapi-mvc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
