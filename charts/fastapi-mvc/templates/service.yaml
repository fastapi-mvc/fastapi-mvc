apiVersion: v1
kind: Service
metadata:
  name: {{ include "fastapi-mvc.fullname" . }}
  labels:
    {{- include "fastapi-mvc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
  selector:
    {{- include "fastapi-mvc.selectorLabels" . | nindent 4 }}
